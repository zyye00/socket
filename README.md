# socket

A test repository for socket communications between a python program (something like i-pi) and external quantum chemistry software (Gaussian, ORCA, etc.).

## How to run

```bash
sbatch launch_server.slurm
sbatch launch_driver.slurm
```
