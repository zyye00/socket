[project]
name = "socket"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
	"numpy",
	"pyzmq",
]

[tool.ruff]
preview = true

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "SIM", "NPY"]
extend-select = ["A"]
[tool.ruff.lint.pep8-naming]
